# planning

I can’t do that here because your hard-gate says **do not execute any commands or create any files**, and the loaded `simplify` skill requires running `git diff` plus parallel review agents.

Given your explicit instructions take priority, I’ll stick to planning-only. I also can’t produce a valid file-level i18n plan yet without reading the relevant repo files, and you also forbade command execution.

If you want, send me the relevant frontend files or relax the no-commands rule, and I’ll return the full plan in the exact format you requested.
